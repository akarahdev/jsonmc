package me.endistic.jsonmc.tree.values;

import org.bukkit.entity.Entity;

public class EventTargets {
    public Entity defaultTarget;

    public EventTargets(Entity defaultTarget) {
        this.defaultTarget = defaultTarget;
    }
}
